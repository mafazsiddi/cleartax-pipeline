services:
  postgres:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: mira
    ports:
      - '5432:5432'
    volumes:
      - mira-postgres-data:/var/lib/postgresql/data

volumes:
  mira-postgres-data:
